# Root-level ignores for the StableWage monorepo

# OS / Editor junk
.DS_Store
Thumbs.db
.vscode
.idea

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Env / secrets (handled here too for safety)
.env
.env.local
.env.*.local
*.pem

# Node modules (ignore root + workspace installs)
node_modules/

# Lockfiles (keep only the one you actually use)
package-lock.json


# Build / temp outputs (generic, workspace-specific ones are inside their own .gitignore)
dist/
build/
out/
temp/
